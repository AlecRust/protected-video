parameters:
  tasks:
    phpparser:
      visitors:
        forbidden_function_calls:
          blacklist:
            - "var_dump"
            - "print_r"
    phpversion:
      project: "7.0"
    # phpcs:
    #   standard: ["WordPress"]
    # phpmd:
    #   ruleset: ["cleancode", "naming"]
    phpmnd:
    # phpstan:
    #   memory_limit: 512M

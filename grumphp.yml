grumphp:
    tasks:
        phpversion:
            project: '7.0'
        # phpcs:
        #   standard: ['WordPress']
        phpmd:
            ruleset: ['cleancode', 'naming']
        phpmnd:
        phpstan:
            memory_limit: 1G

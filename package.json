{
  "name": "protected-video",
  "version": "1.1.9",
  "private": true,
  "description": "YouTube/Vimeo player that prevents easy sharing of the video.",
  "main": "admin/js/protected-video-block.js",
  "repository": "https://github.com/AlecRust/protected-video",
  "author": "Alec Rust <me@alecrust.com>",
  "license": "GPL-2.0-or-later",
  "keywords": [
    "WordPress",
    "block"
  ],
  "scripts": {
    "build": "rollup -c && wp-scripts build src/protected-video-block.js --output-path=admin/js",
    "watch": "rollup -c -w & wp-scripts start src/protected-video-block.js --output-path=admin/js",
    "lint": "prettier '**/*.{js,php,md}' --write; eslint '**/*.js' --fix",
    "release": "release-it --ci",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.16.3",
    "@wordpress/scripts": "^16.1.2",
    "auto-changelog": "^2.3.0",
    "eslint": "^7.28.0",
    "eslint-config-prettier": "^8.3.0",
    "get-video-id": "^3.2.0",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.1",
    "release-it": "^14.8.0",
    "replace": "^1.2.1",
    "rollup": "^2.51.1",
    "rollup-plugin-terser": "^7.0.2"
  },
  "lint-staged": {
    "*.{js,php,md}": "prettier --write",
    "*.js": "eslint --fix"
  }
}
